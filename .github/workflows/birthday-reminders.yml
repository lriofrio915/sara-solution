name: Birthday Reminders

# Requiere secrets en GitHub → Settings → Secrets and variables → Actions:
#   APP_URL     = https://www.consultorio.site
#   CRON_SECRET = (valor del .env CRON_SECRET)

on:
  schedule:
    - cron: '0 12 * * *'  # 7:00 AM Ecuador (UTC-5)
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger birthday-reminders endpoint
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/cron/birthday-reminders")
          HTTP_BODY=$(echo "$RESPONSE" | head -n -1)
          STATUS=$(echo "$RESPONSE" | tail -n1)
          echo "Response status: $STATUS"
          echo "Response body: $HTTP_BODY"
          if [ "$STATUS" != "200" ]; then
            echo "ERROR: endpoint returned $STATUS"
            exit 1
          fi
