// Analytics IA — accessible for all plans including FREE
export const dynamic = 'force-dynamic'

export default function AnalyticsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
